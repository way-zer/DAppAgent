{
  "name": "preload",
  "private": true,
  "main": "dist/index.cjs",
  "scripts": {
    "build": "vite build",
    "dev": "echo 'included in renderer watch'",
    "typecheck": "tsc --noEmit",
    "genDts": "dts-cb -i src/**/*.ts -o exposedInMainWorld.d.ts"
  },
  "dependencies": {
  },
  "devDependencies": {
    "dts-for-context-bridge": "0.6.2"
  }
}
